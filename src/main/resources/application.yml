server:
  port: 8084

spring:
  application:
    name: Users
  datasource:
    url: jdbc:mysql://localhost:3306/wookie_books
    username: wookie
    password: wookie_books
  jpa:
    generate-ddl: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8083/eureka
